import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
    palette: {
        type: 'dark',
        primary: {
            main: '#ccdb38',
            light: '#ffff6d',
            dark: '#98aa00',
        },
    },
});

export default theme;
